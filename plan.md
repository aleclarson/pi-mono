1. Modify `offline-fallback.md` to check if `ctx.model?.provider === FALLBACK_PROVIDER` at the very beginning of the `before_agent_start` handler, before attempting to fetch `1.1.1.1`.
2. Review the file contents.
3. Reply to the PR comment and confirm the change.
4. Submit the changes to the same branch (`docs-offline-fallback`).
